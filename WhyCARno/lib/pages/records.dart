import "package:flutter/material.dart";

class Records extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("녹화 영상"),
      ),
      body: Center(child: Text("녹화 영상 페이지")),
    );
  }
}